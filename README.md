# Localz
Original quick-commerce Flutter app starter.

## Run
1. Install Flutter SDK and Android Studio.
2. Open this folder in Android Studio.
3. Run `flutter pub get`.
4. Run `flutter run`.
5. For release APK: `flutter build apk --release`.

This project intentionally uses original Localz branding and UI rather than copying another app's assets or exact interface.
